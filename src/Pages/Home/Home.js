import React from 'react';
import useTitle from '../../Hooks/UseTitle/UseTitle';

const Home = () => {

    useTitle('Home');

    return (
        <div>
            
        </div>
    );
};

export default Home;